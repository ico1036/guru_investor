# Investment Guru Multi-Agent Discussion System

**Claude Agent SDK**의 `AgentDefinition` 아키텍처를 기반으로 구축된 진정한 멀티 에이전트 투자 토론 시스템입니다.

사회자(Orchestrator)가 5명의 전설적인 투자 거장(서브 에이전트)들을 자유자재로 소환하여, 사용자가 제시한 주제에 대해 심도 있는 토론을 진행합니다.

## 🌟 주요 기능

*   **진정한 멀티 에이전트**: 단일 LLM의 역할 놀이가 아닌, `Task` 도구를 통해 독립된 인스턴스로 실행되는 서브 에이전트들이 협업합니다.
*   **Dynamic Guru Generation (무한 확장)**:
    *   미리 정의된 5명의 거장(워렌 버핏, 피터 린치 등)뿐만 아니라, **사용자가 입력하는 그 어떤 인물**도 즉석에서 에이전트로 소환할 수 있습니다.
    *   예: "일론 머스크", "세종대왕", "사토시 나카모토" 등
*   **실시간 중계**: 오케스트레이터가 누구에게 마이크를 넘기는지(`Task` 호출), 어떤 검색을 하는지 실시간으로 보여줍니다.
*   **결과 저장**: 토론이 끝나면 상세한 내용을 담은 마크다운(`.md`) 파일이 자동 생성됩니다.

## 🚀 시작하기

### 1. 사전 준비

이 시스템은 `claude-agent-sdk`를 사용하며, 로컬 인증이 필요합니다.

```bash
# 1. Claude 인증 (최초 1회)
anthropic auth login

# 2. 의존성 설치
uv sync
```

### 2. 실행 방법

**기본 실행 (AI 주제)**
```bash
uv run run.py
```

**원하는 주제로 토론**
```bash
uv run run.py --topic "비트코인 1억 돌파, 지금이라도 사야 하나?"
```

**원하는 패널(거장) 소환**
```bash
# 일론 머스크와 워렌 버핏의 끝장 토론
uv run run.py --topic "암호화폐의 미래" --gurus "elon_musk,warren_buffett"

# 역사적 인물들의 경제 토론
uv run run.py --topic "자본주의의 미래" --gurus "adam_smith,karl_marx,keynes"
```

## 🏗️ 시스템 아키텍처

### Core Components

1.  **Orchestrator (`run.py`, `orchestrator.py`)**:
    *   전체 토론을 진행하는 사회자입니다.
    *   `Task` 도구를 사용하여 적절한 시점에 서브 에이전트를 호출합니다.
2.  **Sub-Agents (`investment_gurus.py`)**:
    *   각 투자자의 페르소나를 가진 전문가 에이전트입니다.
    *   **Dynamic Prompting**: 모르는 인물이 입력되면, 스스로 웹 검색을 통해 해당 인물의 철학을 학습하고 연기하도록 프롬프트가 자동 생성됩니다.
3.  **Claude Agent SDK**:
    *   `AgentDefinition`을 통해 메인 에이전트에게 서브 에이전트의 존재를 알리고, 도구 호출 시 자동으로 인스턴스를 생성/관리합니다.

### Workflow

```mermaid
[User] -> (Topic & Guru Names) -> [Run.py]
                                     |
                                     v
                           [Orchestrator Agent]
                                     |
               +---------------------+---------------------+
               | (Task Tool: "Hey Buffett...")             | (Task Tool: "Hey Musk...")
               v                                           v
       [Sub-Agent: Buffett]                        [Sub-Agent: Musk]
       (Persona Loaded)                            (Dynamic Search & Persona)
               |                                           |
               +------------------> [Orchestrator] <-------+
                                     |
                                     v
                            (Final Markdown Report)
```

## 📝 결과물

토론이 종료되면 현재 디렉토리에 결과 파일이 생성됩니다.

*   파일명: `discussion_result_YYYYMMDD_HHMMSS.md`
*   내용: 토론 주제, 참여자, 대화 로그, 결론 등

## ⚠️ 면책 조항

이 시스템은 AI 에이전트 기술을 활용한 교육 및 엔터테인먼트 목적의 프로젝트입니다. **생성된 투자 조언은 실제 투자 권유가 아니며, 이에 따른 투자 결과에 대해 책임지지 않습니다.** 실제 투자는 반드시 전문가와 상의하십시오.
